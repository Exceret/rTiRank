% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/6_generate_val.R
\name{generate_val}
\alias{generate_val}
\title{Splits the bulk expression and clinical data into training and validation sets}
\usage{
generate_val(
  bulk_exp,
  bulk_clinical,
  validation_proportion = 0.15,
  mode = c("Classification", "Cox", "Regression"),
  seed = 123L,
  save_path = NULL,
  ...
)
}
\arguments{
\item{bulk_exp}{Matrix or data frame of bulk expression data (genes x samples).}

\item{bulk_clinical}{Data frame of clinical data with samples as rows.}

\item{validation_proportion}{Numeric value between 0 and 1 specifying the
proportion of samples to allocate to the validation set. Default is 0.15.}

\item{mode}{Character string specifying the analysis mode. Must be one of
"Classification", "Cox", or "Regression". Default is "Classification".}

\item{seed}{Integer seed for reproducible random sampling. Default is 123L.}

\item{save_path}{Optional character string specifying the directory path
to save the split datasets as pickle files. If NULL, datasets are not saved.}

\item{...}{Additional arguments (currently unused).}
}
\value{
A list containing four elements:
\item{bulk_exp_train}{Training set expression data (genes x samples)}
\item{bulk_clinical_train}{Training set clinical data}
\item{bulk_exp_val}{Validation set expression data (genes x samples)}
\item{bulk_clinical_val}{Validation set clinical data}
}
\description{
Loads the full bulk expression and clinical data, combines them, performs a
random split, and saves the training and validation sets back to disk in
the '2_preprocessing/split_data' directory.
}
